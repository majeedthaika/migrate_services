services:
  - type: web
    name: smoothexit-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: smoothexit-data
      mountPath: /app/data
      sizeGB: 1
    healthCheckPath: /api/health
